# git

```
git add .
git commit .
git push origin
git pull

git status                          check the current status
git log                            show commit logs
git reflog                          Manage reflog information   record when the tips of branches and other references were updated in the local repository


git log --name-only --oneline

git checkout d387abf -- resources/integration/authcredentials.key

git reset --hard 7ff507d              git reset --hard HEAD




```
