# 系统功能

模拟终端 alacritty

改桌面背景 feh

查看图片 ristretto

alien 将rpm包转换成deb包

制作hexdump或者反过来 xxd   感觉xxd比hexdump要好用一点，可能有误解

hexdump 每两个字节好像得反过来读

想要在ssh断开连接后，机器仍然执行命令 nohup    screen    tmux 都可以，nohup应该是原生为解决这个问题而设计的

ldd - print shared object dependencies             pwn 本地可以用      找libc address

